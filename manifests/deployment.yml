apiVersion : apps/v1
kind: Deployment
metadata:
  name: "recogaks-f203"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "recogaks-f203"
  template:
    metadata:
      labels:
        app: "recogaks-f203"
    spec:
      containers:
        - name: "recogaks-f203"
          image: "acrecog1.azurecr.io/recogaks"
          ports:
          - containerPort: 8000