apiVersion : apps/v1
kind: Deployment
metadata:
  name: "recogaks-ecf8"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "recogaks-ecf8"
  template:
    metadata:
      labels:
        app: "recogaks-ecf8"
    spec:
      containers:
        - name: "recogaks-ecf8"
          image: "acrecog1.azurecr.io/recogaks"
          ports:
          - containerPort: 8000