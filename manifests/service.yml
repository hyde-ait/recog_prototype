apiVersion: v1
kind: Service
metadata:
    name: "recogaks-backend"
    labels:
        app: "recogaks-backend"
spec:
    type: LoadBalancer
    ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
    selector:
        app: "recogaks-backend"