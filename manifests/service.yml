apiVersion: v1
kind: Service
metadata:
    name: "recogaks-28b1"
    labels:
        app: "recogaks-28b1"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "recogaks-28b1"