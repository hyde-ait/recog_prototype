apiVersion: v1
kind: Service
metadata:
    name: "akscluster-9dda"
    labels:
        app: "akscluster-9dda"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "akscluster-9dda"