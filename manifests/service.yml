apiVersion: v1
kind: Service
metadata:
    name: "recogaks-f203"
    labels:
        app: "recogaks-f203"
spec:
    type: LoadBalancer
    ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
    selector:
        app: "recogaks-f203"