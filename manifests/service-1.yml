apiVersion: v1
kind: Service
metadata:
    name: "recogaks-fc9d"
    labels:
        app: "recogaks-fc9d"
spec:
    type: LoadBalancer
    ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
    selector:
        app: "recogaks-fc9d"